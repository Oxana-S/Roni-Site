# Промт для следующей сессии

Скопируй и вставь это в начало новой сессии:

---

Продолжаем работу над сайтом писателя Ростислава Вишневского
(`/Users/sergioiarcov/Documents/PROJECTS-/Project. Roni Site`).

Это статический сайт на движке dc-runtime (`support.js`), 4 языка (RU/EN/UK/BG),
дизайн-токены в `styles/theme.css`. Репозиторий: github.com/Oxana-S/Roni-Site,
превью: https://oxana-s.github.io/Roni-Site/. Каждое изменение коммить и пушь в `main`.

Прежде чем начинать — прочитай:
- `docs/STATUS.md` — что сделано и что осталось;
- `docs/API.md` — как устроен движок/компоненты/токены/i18n (+ §10 бэклог);
- память проекта (architecture-dc-runtime.md, deploy-github-pages.md).

Сейчас приоритеты (выбери, что актуально):
1. Если я пришлю **аудио** (`chapter-1.mp3`) или **картинки** (book-2, world-earth/tolus,
   blog-1..6 — размеры в `docs/IMAGES-spec.md`) — обработай и поставь, убери заглушки.
2. Если назову **домен** — прогони `python3 scripts/set-domain.py https://домен` и подготовь к серверу.
3. Если дам **Google Analytics ID** — впиши в `assets/analytics.js`.
4. Иначе — добиваем минорный бэклог (`docs/API.md` §10) или то, что я скажу.

Правила: правки токенами (не хардкод), проверяй в браузере перед коммитом, не ломай
адаптив и i18n. Отвечай на русском.

---

(Конец промта. Полный контекст всегда в `docs/STATUS.md`.)
